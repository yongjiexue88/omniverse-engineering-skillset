# OpenCode Install

This repository includes an OpenCode plugin entrypoint at:

```txt
.opencode/plugins/omniverse-engineering-skillset.js
```

The plugin registers the repository's `skills/` directory as an OpenCode skill path.
